import { IVNode } from '../interface';
import { History, createHashHistory } from './history';

// interface IRouter extends Props {
// 	history: History;
// }

// interface Props {
// 	children?: IVNode;
// }

// export const Router = ({ children }: IRouter) => {};

// export const HashRouter = (props: Props) => {
// 	return <Router history={createHashHistory()}>{props.children}</Router>;
// };
